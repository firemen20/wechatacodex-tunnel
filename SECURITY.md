# 安全策略

## 这个项目处理的是什么

本工具会生成**代理节点配置**。请理解一件事：

> **`vless://<uuid>@<ip>:<port>` 这个链接本身就是凭据。**
> UUID 就是节点密码，没有第二道验证。分享配置 = 分享节点。

因此本项目对「什么是敏感数据」有明确定义，并做了多重防护。

---

## 什么是敏感数据

| 数据 | 在哪 | 泄露后果 |
|---|---|---|
| `config.json` | 项目根目录 | 含你的 UUID、Worker 名、域名 |
| `dist/*` | 输出目录 | 含完整节点链接 = 节点密码 |
| `data/*` | 工作目录 | 含实测产物、内核、CA 证书 |
| API Token | 环境变量 | 可操作你的 Cloudflare / GitHub 账号 |

**以上全部已在 `.gitignore` 中排除**，CI 里还有一步独立的检查兜底。

---

## 报告漏洞

如果你发现了**安全相关的缺陷**（例如：某种情况下会把凭据写进日志、生成的配置会泄漏到不该去的地方），**请不要开公开 issue**。

请通过以下方式私下联系：

- 在 [Issues](https://github.com/firemen20/wechatacodex-tunnel/issues) 里开一个 **不包含任何细节** 的 issue，说明「有安全问题，需要私下沟通」
- 我会回复一个私下渠道

**请包含：**
- 受影响的版本 / 提交
- 复现步骤
- 影响范围（能拿到什么）

**请一定不要**在报告里粘贴真实的 UUID、Token 或订阅链接 —— 用 `xxxx` 代替。

---

## 使用者的安全责任

本工具只负责**生成配置**，不负责你的部署安全。请自行注意：

1. **`config.json` 和 `dist/` 绝不外传**，别提交、别发群、别传网盘
2. **用不可猜的路径**如果要把配置放到公网（URL 即凭据）
3. **API Token 用完即删** —— 只通过环境变量传，不要写进文件
4. **怀疑 UUID 泄露** → 改 Worker 的 `UUID` 环境变量并重新部署，旧 UUID 立即失效
5. **不要把配置放在 `/sdcard`**（Android）—— 那是共享存储，任何有存储权限的 App 都能读到

---

## 本项目的安全设计

| 措施 | 位置 |
|---|---|
| `.gitignore` 排除敏感文件 | 仓库根目录 |
| CI 独立扫描（防止 `.gitignore` 被绕过） | `.github/workflows/ci.yml` |
| 发布脚本二次过滤 + 泄密扫描 | 见 README 的「发布」一节 |
| 占位符识别（示例 UUID 不误报） | 同上 |
| 代码中零硬编码凭据 | 全项目 |
